import React from "react";
import styled from "styled-components";

const About = () => {
  return <Wrapper className="container">About</Wrapper>;
};

export default About;

const Wrapper = styled.section``;
